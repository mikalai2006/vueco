import { ref } from "vue";
import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";

import {
  LMenu,
  LMenuButton,
  LMenuItem,
  LMenuItems,
  LMenuItemWrapper,
} from "@/components/LMenu";

<Meta
  title="MDX/LMenu"
  component={LMenu}
  subcomponents={{ LMenuButton, LMenuItem, LMenuItems, LMenuItemWrapper }}
  argTypes={{
    modelValue: "model",
  }}
/>

export const Template = (args) => ({
  components: { LMenu },
  setup() {
    const activeItem = ref("");
    return { args, activeItem };
  },
  template: '<LMenu v-model="activeItem" v-bind="args" />',
});

# LBtn

<Canvas>
  <Story name="simple-lmenu" args={{ label: "LMenu" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={LMenu} />
